# HermesGo 验证报告

**文档编号**：014

**生成者**：AI Codex GPT-5.4

**生成日期**：2026-04-16-1005

**最后修改**：2026-04-16-1005

**参考文档**：
- docs/011-2026-04-16-绿色便携版HermesGo-Docker优先方案.md
- docs/012-2026-04-16-HermesGo镜像缓存与导出导入方案.md
- docs/013-2026-04-16-HermesGo单一配置与自检脚本方案.md

**目的**：把 HermesGo 的自检结果固定成可见证据，避免只在对话里说“成功了”，但目录里没有明确记录。

**状态**：验证的是旧版带 tar 的阶段性方案，当前使用版见 `docs/015`。

## 1. 验证方式

1.1 执行 `HermesGo\Verify-HermesGo.bat -KeepTemp`

1.2 验证脚本内部使用本地 `docker.cmd` 桩，模拟 Docker 行为。

1.3 验证覆盖三条路径：

1.3.1 缓存 tar 存在时，先 `docker load`

1.3.2 缓存 tar 不存在时，走 `docker pull`

1.3.3 导出脚本执行 `docker pull` + `docker save`

## 2. 可见结果

2.1 自检命令输出：

```text
Verify step 1: load branch with cached tar files.
HermesGo launcher started.
Ensuring image: ollama/ollama:latest
Loading image from archive: E:\AI\hermes\HermesGo\images\ollama_ollama_latest.tar
Image loaded from archive: ollama/ollama:latest
Ensuring image: ghcr.io/nousresearch/hermes-agent:latest
Loading image from archive: E:\AI\hermes\HermesGo\images\hermes_agent_latest.tar
Image loaded from archive: ghcr.io/nousresearch/hermes-agent:latest
Docker Compose detected: docker compose
Hermes container launched.
Test mode: browser launch skipped.
Verify step 2: pull branch without cached tar files.
HermesGo launcher started.
Ensuring image: ollama/ollama:latest
Image not found locally, will pull: ollama/ollama:latest
Image pulled from registry: ollama/ollama:latest
Ensuring image: ghcr.io/nousresearch/hermes-agent:latest
Image not found locally, will pull: ghcr.io/nousresearch/hermes-agent:latest
Image pulled from registry: ghcr.io/nousresearch/hermes-agent:latest
Docker Compose detected: docker compose
Hermes container launched.
Test mode: browser launch skipped.
Verify step 3: export flow.
Export started.
Export completed.
HermesGo verification passed.
```

2.2 当前目录中的可见产物：

- `HermesGo/Verify-HermesGo.bat`
- `HermesGo/Verify-HermesGo.ps1`
- `HermesGo/HermesGo.config.bat`
- `HermesGo/Export-HermesGoImages.bat`
- `HermesGo/HermesGo.bat`
- `HermesGo/docker-compose.yml`
- `HermesGo/data/hermes/config.yaml`

## 3. 结论

3.1 HermesGo 的启动链路、自检链路、导出链路都已在当前机器上完成模拟验证。

3.2 当前没有真实 Docker，因此没有做真容器运行，但关键路径已经能在本地模拟成功。

3.3 这份报告补上了“你能直接看到”的证据，不再只靠对话描述。
