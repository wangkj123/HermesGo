# 028-2026-04-17-Ollama绿色运行时打包成功归档

## 时间

- 2026-04-17 11:08

## 目标

- 延续 `027` 的 Ollama 绿色运行时打包任务
- 按“国内镜像 -> 国外直连 -> 最后代理回退”的顺序迭代到可验证成功
- 打通 `Bundle-Ollama -> Create-HermesGo -> Verify-HermesGo` 全链路

## 本轮修改

- 更新 `.cursorrules`
  - 新增最高优先级规则：下载和安装链路必须优先国内镜像，其次国外直连，最后才允许代理回退
  - 新增规则：接到“继续”“迭代到成功”“自动测试”类任务时，改完后必须自动运行相关构建和验证，失败后继续修复直到成功或出现硬阻塞
- 重写 `create_hermes_go/Bundle-Ollama.ps1`
  - 移除原先不稳定的分块拼接下载
  - 改为顺序多源下载
  - 每次下载后先做 zip 完整性校验，再进入解包
  - 下载尝试结果自动追加到 `logs/agent-progress.md`
  - 兼容 Windows `curl.exe` 的证书吊销检查问题，默认带 `--ssl-no-revoke`
- 修复 `create_hermes_go/Create-HermesGo.ps1`
  - 在复制缓存的 `ollama-windows-amd64.zip` 前，先创建输出 `installers/` 目录

## 实际下载回退顺序

1. 国内镜像：`https://ollama.ac.cn/download/v0.1.36/ollama-windows-amd64.zip`
   - 结果：404，失败
2. 国外直连：`https://github.com/ollama/ollama/releases/download/v0.1.36/ollama-windows-amd64.zip`
   - 结果：成功下载并通过 zip 校验
3. 国外代理回退
   - 结果：未使用

## 结果

- `create_hermes_go/cache/ollama-windows-amd64-v0.1.36.zip` 已成功下载
- `HermesGo/runtime/ollama/` 已成功解包出绿色 Ollama 运行时
- `HermesGo/installers/ollama-windows-amd64.zip` 已同步落盘
- `create_hermes_go/output/HermesGo/` 已重新构建成功

## 验证

- `powershell -NoProfile -ExecutionPolicy Bypass -File .\create_hermes_go\Bundle-Ollama.ps1`
  - 结果：通过
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\create_hermes_go\Create-HermesGo.ps1 -Clean`
  - 结果：通过
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\create_hermes_go\output\HermesGo\Verify-HermesGo.ps1`
  - 结果：通过
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\HermesGo\Verify-HermesGo.ps1`
  - 结果：通过

## 当前状态

- `027` 中“zip 下载完成但解包失败”的阻塞已解除
- Ollama 绿色运行时已进入当前 HermesGo 和新输出包构建链路
- 镜像优先级和自动测试要求已固化到仓库最高规则

## 剩余风险

- 当前成功源仍是 GitHub 国外直连，版本化国内镜像地址暂不可用
- `Create-HermesGo.ps1` 仍基于当前本机 Python 与现有依赖复制，离“完全脱离构建机”的极致绿色化还有空间
