# HermesGo 三 UI 绿色包解包验证归档

**文档编号**：072

**生成日期**：2026-05-18

**最后修改**：2026-05-18

**状态**：active

**所属主题**：HermesGo 绿色包 / 三 UI / gateway 启动顺序 / 解包自测

**参考文档**：

- `docs/054-2026-04-24-HermesGo七个UI逐个试跑截图归档.md`
- `.cursor/rules/hermesgo-packaging-selftest.mdc`
- `HermesGo/scripts/smoke_3ui_from_zip.ps1`

**目的**：记录 `v0.14-green-3ui-slim` 解包后的三件套（Dashboard 9119、WebUI 8787、Desktop 9120 嵌入）自测结果与截图位置。

## 1. 发布物

| 项 | 值 |
|----|-----|
| ZIP | `dist/HermesGo-2026.05.18-223518-v0.14-green-3ui-slim.zip`（约 218.5 MB） |
| 解包目录 | `HermesGo-slim-verify-20260518/HermesGo/` |
| 认证 | 首次运行从 `%USERPROFILE%\.hermes\auth.json` 导入；`portable_bootstrap` 将 slim 默认对齐为 **openai-codex** |

## 2. 自测结论

| 检查 | 结果 |
|------|------|
| 启动顺序 gateway → Dashboard → WebUI → Desktop | 通过 |
| `smoke_portable_desktop.py` | 通过（gateway_running、9120 HTTP） |
| `smoke_portable_connect.py` | 通过（9119/8787、Codex runtime、`/api/providers/test`） |
| Dashboard Gateway 状态页 | **RUNNING**（PID 见当时运行实例） |
| WebUI 8787 | 可打开，Kanban boards=1 |
| Desktop 嵌入 Dashboard 9120 | 可打开，与 9119 同 UI |

## 3. 本轮修复（写入源码后重打包）

1. `Start-HermesGo.ps1`：先 `Start-GatewayProcess`，再 Dashboard/Desktop；Dashboard 用正确 `HERMES_HOME` 重启陈旧 9119。
2. `gateway/status.py`：Windows `OpenProcess` 检活 + 路径规范化识别 `hermes_cli\main.py gateway`。
3. `gateway/run.py`：`--replace` 等待旧进程退出改用 `_pid_is_alive`。
4. `hermes_cli/web_server.py`：Desktop 嵌入 `/api/ws` + `embedded_chat`。
5. `scripts/smoke_3ui_from_zip.ps1`、`check_dashboard_gateway.py`：一键解包验证。

## 4. 截图归档

路径：`exports/ui-trials/2026-05-18-3ui-slim/`

| 文件 | 说明 |
|------|------|
| `01-dashboard-status.png` | Dashboard 9119 Status（Gateway RUNNING） |
| `02-webui-8787.png` | WebUI 8787 主界面 |
| `03-desktop-embedded-9120.png` | Desktop 内嵌 Dashboard 9120 Status |

## 5. 复现命令（Windows）

```powershell
$zip = "E:\AI\hermes\dist\HermesGo-2026.05.18-223518-v0.14-green-3ui-slim.zip"
powershell -NoProfile -ExecutionPolicy Bypass -File E:\AI\hermes\HermesGo\scripts\smoke_3ui_from_zip.ps1 -ZipPath $zip
```

## 6. 风险与说明

- 无消息平台 token 时 gateway 仅 cron/本地服务，Dashboard 仍应显示 **Gateway RUNNING**。
- WebUI 首次打开可能弹出 onboarding，不影响 HTTP/health 探测。
- Desktop 窗口本体未单独截图；9120 嵌入 Dashboard 作为 Desktop UI 验收依据。
