# HermesGo 直接使用版验证报告

**文档编号**：016

**生成者**：AI Codex GPT-5.4

**生成日期**：2026-04-16-1026

**最后修改**：2026-04-16-1026

**参考文档**：
- docs/015-2026-04-16-HermesGo直接使用版与hello探测方案.md
- docs/014-2026-04-16-HermesGo验证报告.md

**目的**：记录当前“直接使用包”已经跑通 `hello` 探测，并把可见日志固定下来。

## 1. 验证方式

1.1 执行 `HermesGo\Verify-HermesGo.bat -KeepTemp`

1.2 验证脚本内部使用本地 `docker.cmd` 和 `curl.cmd` 桩，模拟 Docker 和服务响应。

## 2. 可见结果

2.1 命令输出：

```text
Verify step 1: docker branch with hello probe.
HermesGo launcher started.
Portable target: Docker Compose with local volumes under E:\AI\hermes\HermesGo\.
Docker Compose detected: docker compose
Hermes container launched.
Hello probe: waiting for http://localhost:3000
Hello probe succeeded.
Test mode: browser launch skipped.
HermesGo verification passed.
```

2.2 `HermesGo/logs/HermesGo.log` 中的可见记录：

```text
[周四 2026/04/16 10:08:13.75] Hermes container launched.
[周四 2026/04/16 10:08:13.75] Hello probe: waiting for http://localhost:3000
[周四 2026/04/16 10:08:13.83] Hello probe succeeded.
```

2.3 当前目录中保留的验证痕迹：

- `HermesGo/logs/HermesGo.log`
- 验证脚本保留的临时目录 `HermesGoVerify-*`

## 3. 结论

3.1 当前版本已经不是 tar 交付思路，而是直接使用包。

3.2 启动后会主动做 hello 探测，确认本地服务有响应。

3.3 这次验证在当前机器上已经模拟成功，并且留下了可见证据。
