# HermesGo 可见窗口验证报告

**文档编号**：018

**生成者**：AI Codex GPT-5.4

**生成日期**：2026-04-16-1036

**最后修改**：2026-04-16-1036

**参考文档**：
- docs/017-2026-04-16-HermesGo可见状态窗口方案.md
- docs/016-2026-04-16-HermesGo直接使用版验证报告.md

**目的**：记录 HermesGo 的“可见窗口”不是口头说法，而是已经发出了实际启动命令。

## 1. 验证方式

1.1 在非 headless 模式下调用 `HermesGo.bat`

1.2 使用本地 `powershell.cmd`、`docker.cmd`、`curl.cmd` 桩记录实际命令

## 2. 可见结果

2.1 `powershell-stub.log` 记录到的窗口启动命令：

```text
powershell -NoProfile -STA -ExecutionPolicy Bypass -File "E:\AI\hermes\HermesGo\HermesGoStatus.ps1" -LogFile "E:\AI\hermes\HermesGo\logs\HermesGo.log" -Title "HermesGo"
```

2.2 `curl-stub.log` 记录到的 hello 探测命令：

```text
curl -fsS --max-time 5 "http://localhost:3000"
```

2.3 `HermesGo/logs/HermesGo.log` 记录到的启动顺序：

```text
HermesGo launcher started.
Docker Compose detected: docker compose
Hermes container launched.
Hello probe: waiting for http://localhost:3000
Hello probe succeeded.
```

## 3. 结论

3.1 可见状态窗口启动命令已经真实发出。

3.2 该窗口由 `HermesGoStatus.ps1` 提供，默认启动时会被拉起。

3.3 自检仍然保持 headless，不会干扰用户的可见模式。
