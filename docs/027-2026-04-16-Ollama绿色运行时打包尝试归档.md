# 027-2026-04-16-Ollama绿色运行时打包尝试归档

## 时间

- 2026-04-16 23:12:50

## 目标

- 把 Ollama 也收敛为绿色可搬运运行时
- 让 `HermesGo` 最终目录可以直接复制到别的 Windows 11 机器上运行

## 本轮动作

- 新增 `create_hermes_go/Bundle-Ollama.bat`
- 新增 `create_hermes_go/Bundle-Ollama.ps1`
- 修改 `create_hermes_go/Create-HermesGo.ps1`，让输出包可复制 `HermesGo\runtime\ollama`
- 采用官方 Ollama release asset `v0.1.36/ollama-windows-amd64.zip` 作为绿色运行时来源
- 尝试通过分段下载把 zip 落到本地缓存后再解包

## 结果

- 官方 release asset 的分段下载已完成到本地缓存
- 解包时失败，错误为 `End of Central Directory record could not be found`
- 说明当前缓存 zip 仍然不是有效完整压缩包

## 当前状态

- `HermesGo` 的绿色 Ollama 目录尚未最终落地
- `create_hermes_go` 里已经有了可继续迭代的打包脚本
- 下一步只需要修复下载源或下载方式，再次打包即可
