---
description: HermesGo 绿色包打包、启动、自测、Provider 已知坑（持续追加）
globs: HermesGo/**,create_hermes_go/**,dist/**,**/Start-HermesGo.ps1,**/build_zip*.py,**/smoke_*.py
alwaysApply: false
---

# HermesGo 打包与自测 — 已知坑清单

修改打包/启动/鉴权前 **先读本文件**。修复新问题后按 `lessons-learned-auto-update.mdc` **追加**条目。

## 固定约定（每次必查）

| 项 | ❌ 错误 | ✅ 正确 |
|----|--------|--------|
| Codex provider ID | `openai_codex` | `openai-codex` |
| Provider 探测 | 对 `codex_responses` 调 `/v1/models` | 仅 `chat_completions` 探测 models；Codex 返回「已解析 runtime」 |
| Dashboard `/api/*` | 无 Authorization | `Bearer` + 从 `/env?quick=1` HTML 取 `__HERMES_SESSION_TOKEN__` |
| 便携 PATH | 含系统 Python/Anaconda | 仅 `app/runtime/python311`、`app`、`System32` |
| `HERMES_HOME` | 用 `~/.hermes` 或仓库根 | `app/home`（zip 内 `HermesGo/app/home`） |
| `.bat` | UTF-8 BOM | UTF-8 **无 BOM** |
| PowerShell 路径 | 依赖 PATH 的 `powershell` | `%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe` |
| PS 5.1 子进程环境 | `Start-Process -Environment` | `Start-ProcessWithEnvironment`（见 `Start-HermesGo.ps1`） |
| Agent 终端命令 | `git` / `py` / `cmd` 裸调用 | 全路径：`Git\cmd\git.exe`、包内 `python.exe`、上述 `powershell.exe` |
| 自测运行环境 | WSL 里测 COM/便携包 | **Windows 本机** Shell + 包内 Python 绝对路径 |
| WSL 适用范围 | 要求用户装 WSL、或影响 Cursor/系统 | **所有 Hermes 入口**设 `HERMES_DISABLE_WSL=1`（exe/bat/gateway/dashboard/WebUI/Desktop/**cmd hermes**/Chat 窗）；`hermes_green_windows_only()` 兜底；**与 Cursor 无关** |
| zip 根目录 | 散落 scripts/tools | 仅 `HermesGo.exe`、`README.txt`、三个 `.bat`；其余在 `app/` |
| 产物位置 | `build_zip_slim.py` 复制 zip 到桌面 | 只输出 `dist/`；测试树用 `packaging_sync` 同步，**不要**拷到 Desktop |
| dist 清理 | 保留每次构建的全部历史 zip | `build_zip_slim.py` 构建后 `prune_old_slim_zips` 只留最新 slim zip + `README.txt` |
| 版本号 | zip 名 `v0.14.5` 但 `__version__` 仍 `0.14.2` | `packaging_release.SLIM_VERSION` + `ensure_runtime_version()` 打包前对齐 |
| sync 校验 | 构建后不验证测试树 | `packaging_sync` 结束后 `verify_test_package_matches_zip`；失败 exit 1 |
| 全量冒烟 | `Test-NetConnection` 等端口、Desktop 重复起 bat | `run_full_smoke_iter.ps1` 用 `Test-TcpPort`；Desktop 置 `HERMESGO_VERIFY_DESKTOP_ONLY=1` 且放最后 |
| Windows 保留名 | zip 含 `nul` 等 | `packaging_safe.py` 跳过/校验；启动时 `Remove-WindowsReservedArtifacts` |

## 已记录教训（按时间追加）

### 2026-05-18 — Codex provider 测试 502
- **现象**：`/api/providers/test` 对 openai-codex 返回 502
- **错误做法**：`api_mode not in ("chat_completions", "codex_responses")` 时对 Codex base 调 `/v1/models`
- **正确做法**：`api_mode not in ("chat_completions",)`；`codex_responses` 只报告 resolved runtime
- **涉及文件**：`hermes_cli/web_server.py`（`runtime/hermes-agent` 与 `hermes_cli` 两处需同步）

### 2026-05-18 — 配置 provider 拼写
- **现象**：模型路由无效、runtime 解析异常
- **错误做法**：`config.yaml` 写 `provider: "openai_codex"`
- **正确做法**：`provider: "openai-codex"`（与 `models.py` / `runtime_provider.py` 一致）

### 2026-05-18 — 便携包首次无密钥
- **现象**：解压后 `app/home` 空，无法连模型
- **错误做法**：假设用户手抄 API key；或强依赖 `~/.hermes` 路径
- **正确做法**：`Ensure-PortableAuthFromProfile` 在**无** `app/home/auth.json` 时从 `%USERPROFILE%\.hermes\` 复制一次；默认 slim 模板 `packaging/config.yaml.slim-default` 为 deepseek，Codex 靠 `auth.json`

### 2026-05-18 — Desktop 启动失败（PS 5.1）
- **现象**：`Start-Process` 报 Environment 参数错误
- **错误做法**：`Start-Process -Environment @{...}`
- **正确做法**：`Start-ProcessWithEnvironment` 辅助函数

### 2026-05-18 — 自测误判「未连接」
- **现象**：Dashboard `/api/health` 401，以为服务挂了
- **错误做法**：不带 token 调 `/api/*`
- **正确做法**：根路径 `/` 或 `/health`（若存在）探活；API 用 session token；脚本见 `HermesGo/scripts/smoke_portable_connect.py`

### 2026-05-18 — Codex 已登录但 UI 仍提示未配置
- **现象**：`auth.json` 有 openai-codex，但 Dashboard/WebUI 仍显示需配置
- **错误做法**：只查 `config.yaml` 是否有 provider，忽略 OAuth 已登录
- **正确做法**：`GET /api/setup/status` 或 WebUI `/api/onboarding/status`；Codex 登录 + `config.yaml` 中 `provider: openai-codex` 才为 `ready`

### 2026-05-18 — Desktop 启动顺序
- **现象**：只双击 `Hermes.exe` 或旧 `HermesDesktop.bat`（`start` 无初始化）进不去
- **错误做法**：Desktop 前先起 9119/8787；或 `HermesDesktop.bat` 不跑 `Ensure-PortableAuthFromProfile`
- **正确做法**：`HermesDesktop.bat` → `Start-HermesGo.ps1 -DesktopOnly`（便携 PATH/auth/config → 只起 Desktop → 探测 9120+）；`HermesGo.bat` 仍可同时起三件套

### 2026-05-18 — Desktop 启动失败 `--tui`
- **现象**：Desktop 窗口卡在启动/进不去；`app/home/logs/desktop.log` 有 `unrecognized arguments: --tui`
- **错误做法**：只更新 Electron `Hermes.exe`，不更新捆绑的 `runtime/hermes-agent` CLI
- **正确做法**：在 `hermes_cli/main.py` 的 `dashboard` 子命令增加 `--tui`、`--skip-build`；`cmd_dashboard` 识别 `HERMES_DASHBOARD_TUI`/`HERMES_WEB_DIST`；`HermesDesktop.bat` 用 `start /D runtime\hermes-desktop`

### 2026-05-18 — 构建后测试目录不同步
- **现象**：修了源码但 `HermesGo-slim-test-v014` 仍是旧包
- **错误做法**：只改 git 树不跑 sync
- **正确做法**：`build_zip_slim.py` 结束后 `packaging_sync.sync_test_package_from_zip`；自测前可设 `HERMESGO_TEST_APP_ROOT` 指向 `...\HermesGo\app`

### 2026-05-18 — Windows 启动顺序：先 gateway 再 Desktop/Dashboard
- **现象**：Dashboard 状态页显示 Gateway 未运行；Desktop 无法正常使用
- **错误做法**：`-DesktopOnly` 只起 Desktop；Windows 上 `hermes gateway start` 不支持（会报 Not supported）
- **正确做法**：`Start-HermesGo.ps1` 先 `Start-GatewayProcess`（`python -m hermes_cli.main gateway run --replace --quiet` 后台）；再起 Dashboard 9119，最后 Desktop；`gateway/status.py` 在 Windows 用 `OpenProcess` 检活、路径用 `/` 规范化识别 `hermes_cli\main.py gateway`

### 2026-05-19 — WebUI 显示 Gateway not running / 未连接
- **现象**：8787 WebUI 横幅或 Gateway 卡片显示未运行；Dashboard 9119 却显示 RUNNING
- **错误做法**：`HermesWebUI.bat` 只 `python run.py` 不起 gateway；或 WebUI 进程未继承 `HERMES_HOME=app/home`
- **正确做法**：`HermesWebUI.bat` → `Start-HermesGo.ps1 -WebUIOnly`（先 gateway 再 8787）；`Start-WebUIProcess` 显式 `HERMES_HOME` 并轮询 `/api/health/agent`；`agent_health._gateway_root_pid_path()` 用 `get_hermes_home()`
- **涉及文件**：`HermesWebUI.bat`、`Start-HermesGo.ps1`、`hermes-webui/run.py`、`hermes-webui/api/agent_health.py`

### 2026-05-19 — Desktop 报找不到网关但 Dashboard 显示 Gateway 运行
- **现象**：9119 状态页 Gateway RUNNING；Desktop 提示「找不到网关 / gateway not connected」
- **错误做法**：只修 9119 Dashboard；或假定 Desktop 与浏览器共用同一 `HERMES_HOME`
- **正确做法**：便携包网关 PID 在 `app/home/gateway.pid`；Desktop 子进程未继承 `HERMES_HOME` 时会查 `~/.hermes`；`hermes_constants.detect_portable_hermes_home()` + `cmd_dashboard` 调用 `ensure_portable_hermes_home_env()`；启动器给 Desktop 传 `HERMES_HOME` 与 `HERMES_DASHBOARD_TUI=1`；网关进程显式带 `HERMES_HOME` 启动
- **涉及文件**：`hermes_constants.py`、`hermes_cli/main.py`、`Start-HermesGo.ps1`

### 2026-05-19 — HermesGo.exe 自动更新 v0.14.2 失败
- **现象**：启动器提示发现 `v0.14.2-green-3ui-slim`，点更新后报「网络探测失败」或「文件正由另一进程使用」（如 `hermes-desktop\chrome_100_percent.pak`）
- **错误做法**：对 `releases/download/...zip` 做 4s HEAD 探测失败后直接中止；Desktop/Gateway 未关就覆盖 `app/runtime`
- **正确做法**：`HermesGoBootstrap.cs` 在已成功拉取 GitHub releases API 后跳过重复网络探测；镜像源（`ghfast.top`）优先；下载超时默认 180s；覆盖前 `StopPortableHermesProcessesBeforeUpdate()`；改后重编 `HermesGo\scripts\Compile-HermesGoBootstrap.ps1` 并替换包根 `HermesGo.exe`
- **涉及文件**：`create_hermes_go/HermesGoBootstrap.cs`、`HermesGo/HermesGo.exe`

### 2026-05-18 — Desktop 网关 WebSocket 未启用
- **现象**：`backend is ready`、9120 HTTP 200，但 Desktop 仍卡在 onboarding「No inference provider is configured」；日志有 `embedded_chat_disabled` / WS close **4403**
- **错误做法**：只给 `dashboard` 加 `--tui` CLI 参数，不补 `web_server` 的 `/api/ws` 与 `embedded_chat=True`
- **正确做法**：`runtime/hermes-agent/hermes_cli/web_server.py` 需 `_DASHBOARD_EMBEDDED_CHAT_ENABLED`、`@app.websocket("/api/ws")` → `tui_gateway.ws.handle_ws`；`cmd_dashboard` 在 `HERMES_DASHBOARD_TUI=1` 或 `--tui` 时 `start_server(..., embedded_chat=True)`；打包须含完整 `tui_gateway/` 包
- **Codex 仅登录**：`setup.status` 可为 true 但 `setup.runtime_check` / UI 仍提示未选模型 → `Ensure-PortableAuthFromProfile` 后跑 `hermes_cli/portable_bootstrap.ensure_codex_config_if_authed()`（slim 默认 deepseek 时自动写 `openai-codex`）

## 自测命令（Windows）

```powershell
$env:HERMESGO_HEADLESS = "1"
$env:HERMESGO_ALLOW_HEADLESS = "1"
$env:HERMESGO_LAUNCH = "dashboard,webui"
E:\AI\hermes\HermesGo-slim-test-v014\HermesGo\HermesGo.bat

$env:HERMESGO_TEST_APP_ROOT = "E:\AI\hermes\HermesGo-slim-test-v014\HermesGo\app"
E:\AI\hermes\HermesGo\runtime\python311\python.exe E:\AI\hermes\HermesGo\scripts\smoke_portable_connect.py
```

一键：`HermesGo\scripts\run_portable_selftest.ps1`

Desktop：`HermesGo\HermesDesktop.bat` 或 `scripts\smoke_portable_desktop.py`（**remote 模式**连 9119：日志 `Remote Hermes backend is ready`；勿再要求 9120+ 监听）

### 2026-05-19 — Desktop 不能用：embedded_chat 关 + Codex 配额耗尽
- **现象**：`desktop.log` 显示 remote ready，但 UI 仍 onboarding；`errors.log` 有 `embedded_chat_disabled` close **4403**；Codex `429 usage limit`
- **错误做法**：9119 Dashboard 不带 `--tui`/`HERMES_DASHBOARD_TUI`；有 Codex OAuth 时 `ensure_codex_config_if_authed` 强行改回 openai-codex
- **正确做法**：`Start-DashboardProcess` 加 `--tui` + `HERMES_DASHBOARD_TUI=1`；`ensure_deepseek_config_if_key` 优先于 Codex；Codex exhausted 时改 deepseek；密钥写 `app/home/.env` 的 `DEEPSEEK_API_KEY`
- **涉及文件**：`Start-HermesGo.ps1`、`hermes_cli/portable_bootstrap.py`

### 2026-05-19 — 默认启动须开 8787+9119 浏览器并起 Desktop
- **现象**：双击 `HermesGo.exe` 只起服务、不开浏览器；或 Desktop 探测失败导致两个浏览器都不开
- **错误做法**：仅设 `HERMESGO_HEADLESS=1` 做自测后用户环境继承；先等 Desktop 再开浏览器（Desktop 抛错则浏览器跳过）
- **正确做法**：全量模式先 `Open-HermesBrowserSuite`（8787 + 9119/env）再 `Start-HermesDesktopWithProbe`；headless 须 **同时** `HERMESGO_HEADLESS=1` 与 `HERMESGO_ALLOW_HEADLESS=1`
- **涉及文件**：`Start-HermesGo.ps1`、`scripts/run_portable_selftest.ps1`

### 2026-05-19 — 三件套以 Dashboard 9119 为唯一登录/配置源
- **现象**：Dashboard 已登录、Gateway RUNNING；Desktop 仍 onboarding/找不到网关；WebUI 要再走一遍向导
- **错误做法**：Desktop 自启 9120 嵌入式 dashboard（另一套 session）；各 UI 各自配 provider
- **正确做法**：`Start-HermesDesktopProcess` 在 9119 就绪后从 `/env?quick=1` 取 `__HERMES_SESSION_TOKEN__`，设 `HERMES_DESKTOP_REMOTE_URL` + `HERMES_DESKTOP_REMOTE_TOKEN`；WebUI 用同一 `HERMES_HOME` + `-WebUIOnly` 也先起 9119；探测 Desktop 就绪看 `Remote Hermes backend is ready`
- **涉及文件**：`Start-HermesGo.ps1`

### 2026-05-19 — Codex/Node 环境会让 Desktop 0 码秒退
- **现象**：`Hermes.exe` 启动后立即退出，`Start-HermesGo.ps1` 报 `Hermes Desktop exited before ready (code 0)`，且没有 `app/home/logs/desktop.log`
- **错误做法**：让 Desktop 继承 Agent/Codex shell 里的 `ELECTRON_RUN_AS_NODE=1`
- **正确做法**：`Start-ProcessWithEnvironment` 支持移除环境变量；启动 Desktop 时显式清除 `ELECTRON_RUN_AS_NODE`
- **涉及文件**：`Start-HermesGo.ps1`

### 2026-05-19 — Desktop remote 冒烟不要再要求 9120
- **现象**：Desktop 已通过 9119 remote backend 就绪，但 `smoke_portable_desktop.py` 仍报 `FAIL no listener 9120-9199 after bat`
- **错误做法**：把 9120-9199 本地嵌入式 dashboard 当成唯一成功条件
- **正确做法**：remote 模式以 `desktop.log` 的 `Remote Hermes backend is ready` 或 9119 Dashboard HTTP + Desktop 进程作为成功条件；本地模式才检查 9120-9199
- **涉及文件**：`scripts/smoke_portable_desktop.py`

### 2026-05-19 — Codex 自动对齐必须同时修 provider 和 model
- **现象**：`auth.json` 已登录 Codex，启动后 `provider: openai-codex` 但 `model.default` 仍是 `deepseek-v4-flash`
- **错误做法**：只切 provider/base_url，或 `_update_config_for_provider(default_model=...)` 在已有 model 时不覆盖
- **正确做法**：传入 `default_model` 时必须写入该模型；`portable_bootstrap.ensure_codex_config_if_authed()` 还要纠正已存在的 `openai-codex + 非 Codex model` 坏配置
- **涉及文件**：`runtime/hermes-agent/hermes_cli/auth.py`、`runtime/hermes-agent/hermes_cli/portable_bootstrap.py`

### 2026-05-19 — Desktop 找不到 gateway 的根因闭环

- **现象**：3UI 启动时 Dashboard HTTP 已起来，但 Desktop 提示后端/gateway 不可用，或启动日志里先出现 `Desktop boot failed` 后又出现 `Remote Hermes backend is ready`，导致探测误判。
- **根因**：
  1. `Start-HermesGo.ps1` 只看到已有 `Hermes.exe` 就跳过，可能复用旧环境启动的 Desktop，没有绑定当前便携 `HERMES_HOME`、Dashboard URL 与 session token。
  2. `desktop.log` 没有在重新启动 Desktop 前清理，历史失败行会让 readiness probe 误判。
  3. 打包脚本未把 `scripts/check_dashboard_gateway.py` 放进 `app/scripts/`，一旦启动脚本依赖 Dashboard API `gateway_running=true`，绿色包里会永远判 false。
- **修复**：
  - 发现同包 `Hermes.exe` 已运行时，先重启再注入 `HERMES_DESKTOP_REMOTE_URL` / `HERMES_DESKTOP_REMOTE_TOKEN` / 便携 `HERMES_HOME`。
  - 启动 Desktop 前删除 `app/home/logs/desktop.log`，并让 probe 优先识别 `Remote Hermes backend is ready`。
  - `build_zip_slim.py` 必须把 `check_dashboard_gateway.py` 打入 `HermesGo/app/scripts/`。
- **回归**：从 `HermesGo-slim-test-v014\HermesGo` 冷启动 `HermesGo.bat -NoOpenBrowser -NoOpenChat`，必须看到 `Dashboard reports gateway_running=true`、`WebUI probe succeeded`、`Hermes Desktop probe succeeded (remote backend on 9119)`；再跑 `smoke_portable_connect.py` 与 `smoke_portable_desktop.py`。

### 2026-05-19 — Desktop WS hello 失败（async_utils + target_model）
- **现象**：CLI/Dashboard/WebUI hello 正常，Desktop 发 hello 无回复或 WS 秒断（`ConnectionClosedOK 1000`）；`agent.log` 有 `No module named 'agent.async_utils'` 或 `agent init failed: resolve_runtime_provider() got an unexpected keyword argument 'target_model'`
- **错误做法**：只改 smoke 脚本事件名、不补缺失模块；或保留 `resolve_runtime_provider(..., target_model=...)`
- **正确做法**：新增 `runtime/hermes-agent/agent/async_utils.py`（`safe_schedule_threadsafe`）；`tui_gateway/server.py` 的 `_make_agent` 只传 `requested=`；`smoke_hello_all_ui.py` 识别 `message.delta`/`message.complete`；全量回归：`HERMESGO_TEST_APP_ROOT=...\app` + `smoke_hello_all_ui.py` 四路 ALL passed
- **涉及文件**：`agent/async_utils.py`、`tui_gateway/ws.py`、`tui_gateway/server.py`、`scripts/smoke_hello_all_ui.py`

### 2026-05-19 — Desktop remote API 401（鉴权头不一致）
- **现象**：`HERMES_DESKTOP_REMOTE_*` 已注入、`desktop.log` 有 `Remote Hermes backend is ready`，但 Electron 控制台/`hermes:api` 大量 `401 Unauthorized`；OAuth/Keys 一直转圈或 onboarding 无法完成
- **错误做法**：只让 Dashboard SPA 认 `Authorization: Bearer`；或改 Desktop 去发 Bearer 而不修服务端
- **正确做法**：`web_server.py` 恢复双通道鉴权：`_SESSION_HEADER_NAME = X-Hermes-Session-Token`（Desktop `main.cjs`）+ `Authorization: Bearer`（浏览器 Dashboard）；`_has_valid_session_token()` 供 middleware 与 `_require_token` 共用
- **涉及文件**：`runtime/hermes-agent/hermes_cli/web_server.py`

### 2026-05-19 — DesktopOnly 在 headless 下不启动 Hermes.exe
- **现象**：`HermesDesktop.bat` / `smoke_portable_desktop.py` exit 0 但无 `desktop.log`、`FAIL desktop readiness`；debug 无 `Hermes Desktop started`
- **错误做法**：`Start-HermesDesktopWithProbe` 在 `$headless` 时直接 return（即使用 `-DesktopOnly`）
- **正确做法**：仅当 `$headless -and -not $DesktopOnly` 时跳过 Desktop；`smoke_portable_desktop.py` 运行 bat 前 `pop HERMESGO_HEADLESS/ALLOW_HEADLESS`
- **涉及文件**：`Start-HermesGo.ps1`、`scripts/smoke_portable_desktop.py`

### 2026-05-19 — Desktop 日志 API file=gui 400
- **现象**：Desktop 设置/日志页 `hermes:api` 报 `Unknown log file: gui`
- **错误做法**：只登记 agent/errors/gateway，不映射 Desktop 请求的 `gui`
- **正确做法**：`hermes_cli/logs.py` 的 `LOG_FILES` 增加 `"gui"`/`"desktop"` → `desktop.log`
- **涉及文件**：`runtime/hermes-agent/hermes_cli/logs.py`

### 2026-05-19 — 构建后测试包未同步 / sync 半失败毁目录
- **现象**：`dist` 最新 zip 的 `Start-HermesGo.ps1` 与 `HermesGo-slim-test-v014` 哈希不一致；`packaging_sync` 报 WinError 32；测试树 `app/runtime/hermes-agent` 被删一半
- **错误做法**：只跑 `build_zip_slim.py` 不确认 sync 输出；进程占用时仍 `shutil.rmtree`；用 `Copy-Item` 单文件「补同步」
- **正确做法**：构建后看 `Test package synced`；sync 前结束所有 `HermesGo-*` 下 python/Hermes.exe；失败则换 `--dest` 或先 `taskkill` 再整包 `packaging_sync.py --zip ...`；自测以 zip 哈希为准
- **涉及文件**：`HermesGo/packaging_sync.py`、`HermesGo/build_zip_slim.py`

### 2026-05-19 — 发行 tag 与运行时 __version__ 不一致
- **现象**：zip 名 `v0.14.5-green-3ui-slim`，`hermes_cli/__init__.py` 仍为 `0.14.2`；GitHub 同 tag 多次换包，用户难判断要不要点更新
- **错误做法**：只改 `build_zip_slim.py` 的 `RELEASE_TAG` 不改 `__version__`；同日多次上传同 tag 资产
- **正确做法**：打包前同步 bump `runtime/hermes-agent/hermes_cli/__init__.py`；同 tag 只保留一份 zip 或换新 tag；`ShouldUpdateRelease` 依赖 AgentVersion 解析
- **涉及文件**：`HermesGo/build_zip_slim.py`、`runtime/hermes-agent/hermes_cli/__init__.py`、`HermesGoBootstrap.cs`

### 2026-05-19 — exe 自动更新未保留 workspace/webui-data（已修复）
- **现象**：`packaging_sync` 会保留 `app/workspace`、`app/webui-data`；`HermesGoBootstrap.ApplyUpdate` 只跳过 `app/home|data|logs`
- **错误做法**：假设开发侧 sync 与用户侧 exe 更新保留范围相同
- **正确做法**：`packaging_release.PRESERVE_*` 为唯一列表；`ApplyUpdate` 与 `packaging_sync` 均保留五目录；改 bootstrap 后须 `Compile-HermesGoBootstrap.ps1` 并 `build_zip_slim.py`
- **涉及文件**：`packaging_release.py`、`packaging_sync.py`、`create_hermes_go/HermesGoBootstrap.cs`

### 2026-05-19 — Dashboard DeepSeek 连通 OK 但 CLI 401（****ired）
- **现象**：9119 `/api/providers/test` 成功；`hermes chat -q hello` 报 `Authentication Fails, Your api key: ****ired is invalid`；`app/home/.env` 里 `DEEPSEEK_API_KEY=` 为空
- **错误做法**：只信 Dashboard 进程内 `os.environ`；让 `load_pool` 在 env 空时 prune 掉 `auth.json` 里 `env:DEEPSEEK_API_KEY` 条目；无 key 时仍用 `no-key-required` 调 API
- **正确做法**：`credential_pool._prune_stale_seeded_entries` 保留仍含可用 `access_token` 的 env 种子条目；`_resolve_api_key_provider_secret` 读 `get_env_value` + credential pool；`run_agent` 对 `api_key` provider 无 key 时 fail-fast；`env_loader` 不把空 `*_API_KEY=` 写入进程环境；Dashboard 保存密钥后确认 `app/home/.env` 非空
- **涉及文件**：`agent/credential_pool.py`、`hermes_cli/auth.py`、`hermes_cli/env_loader.py`、`run_agent.py`

### 2026-05-19 — Kanban 能打开但没有任何卡片（no data）
- **现象**：Kanban 面板空白、各列为 Empty；API 200 但 `tasks` 为空
- **错误做法**：以为是 Dashboard 插件坏了；在 dev 树只用 `HermesGo/home/kanban.db` 而数据在 `HermesGo/app/home/kanban.db`
- **正确做法**：`HERMES_HOME` 指向当前活跃的 `home/`（dev=`HermesGo/home`，zip=`app/home`）；启动器 `Sync-PortableKanbanData` 在 home 无任务且 `app/home` 有库时复制 `kanban.db`；新任务用面板「+」或 Dashboard `/kanban` 插件创建
- **涉及文件**：`Start-HermesGo.ps1`、`home/kanban.db`

### 2026-05-19 — Desktop 模式 Kanban「Failed to fetch」
- **现象**：绿色版 Desktop-only 打开 Kanban 显示 `Kanban unavailable: Failed to fetch`；8787 未监听
- **错误做法**：只起 Dashboard(9119) 不起 WebUI(8787)；在 9119 上直接提供 `/api/kanban`（仅 WebUI 有 `kanban_bridge`）
- **正确做法**：Desktop-only 仍 `Start-WebUIProcess`；`web_server.py` 挂载 `/api/kanban/*` → `HERMES_WEBUI_URL`(8787) 代理；启动时 `kanban_db.init_db()` + `HERMES_HOME`/`HERMES_PORTABLE_APP_ROOT`
- **涉及文件**：`Start-HermesGo.ps1`、`hermes_cli/web_server.py`

### 2026-05-20 — 滚到底后仍被拉回中间（unpinned + nearBottom）
- **现象**：用户停在底部看流式输出，消息增多后视口突然上跳；或 WebUI 在底部选字后 `scrollTop` 被旧快照恢复
- **错误做法**：`groupCount` 变化时无条件恢复 `p.current`/`snapshot.top`；WebUI `_restoreMessageScrollSnapshot` 在 `!_scrollPinned` 时仍写回 `scrollTop`
- **正确做法**：Desktop `MNt`：仅当 `scrollHeight-scrollTop-clientHeight > kNt` 时恢复；默认 `useRef(!1)` 不自动 re-pin；WebUI：unpinned 且 `snapshot.nearBottom` 时直接 `return` 不改 `scrollTop`；`kNt=48`
- **涉及文件**：`patch_desktop_scroll.py`、`hermes-webui/static/ui.js`

### 2026-05-20 — EXE 更新须先出文件列表再确认
- **现象**：用户要求更新前看到将改哪些文件，并能确认/跳过/取消；仅「立即更新/稍后」两按钮不够
- **错误做法**：`ApplyReleaseUpdateAsync` 下载后直接 `ApplyUpdate`；启动时 `ShowPrimaryDeferPrompt` 在下载前问一句
- **正确做法**：解包后 `BuildUpdatePlan` → 写入 `app/logs/update-preview.txt` → 三按钮对话框（立即更新/跳过/取消）；`UpdateResult.UserDeclined` 区分失败与用户拒绝
- **涉及文件**：`create_hermes_go/HermesGoBootstrap.cs`

### 2026-05-20 — Desktop 补丁打在错路径（test 仍用旧 asar）
- **现象**：dev 已 v4 但 `HermesGo-slim-test-v014` Desktop 仍跳；`packaging_sync` 后照旧
- **错误做法**：只 patch `HermesGo/runtime/.../app.asar`；打包 zip 实际在 `HermesGo/app/runtime/...`；未在 `build_zip_slim.py` 前跑补丁
- **正确做法**：`patch_desktop_scroll.py` 同时处理 `runtime/` 与 `app/runtime/`；打包前必跑；测试树可传 asar 路径参数；sync 后若 zip 未含 v4 需重打 zip 或单独 patch 测试树 asar
- **涉及文件**：`HermesGo/scripts/patch_desktop_scroll.py`、`HermesGo/build_zip_slim.py`

### 2026-05-20 — Desktop 聊天仍来回跳（虚拟列表 + 补丁策略）
- **现象**：流式输出时视口上下抖；中间阅读被拽动；与 WebUI 表现不一致
- **错误做法**：在压缩 bundle 里保留 `ResizeObserver→scrollTop=scrollHeight` + `groupCount` 变化时恢复旧 `scrollTop`（与 TanStack Virtual `TNt` 量测冲突）；用正则补丁迭代 `kNt` 但不改跟底机制
- **正确做法**：MNt v4 去掉 ResizeObserver 跟底与 groupCount 回滚；仅在 **用户 pinned** 时用 `requestAnimationFrame` + `virtualizer.scrollToIndex(last,{align:end})` 跟底；长期应在 `apps/desktop` 源码修并 `build_hermes_desktop_portable.ps1` 重打包，勿只改 asar
- **涉及文件**：`HermesGo/scripts/patch_desktop_scroll.py`

### 2026-05-20 — Desktop 白屏（MNt 补丁 ResizeObserver 笔误）
- **现象**：Hermes Desktop 窗口空白；`desktop.log` 仅显示 remote backend ready
- **错误做法**：`patch_desktop_scroll.py` 写成 `t.firstElementChild&&r.observe(...)`（`r` 是 `requestAnimationFrame` 句柄）
- **正确做法**：应为 `i.observe(t.firstElementChild)`；补丁脚本在已打过补丁时须检测 `i.observe` 再跳过；修后重跑 `patch_desktop_scroll.py` 并同步 `app.asar`
- **涉及文件**：`HermesGo/scripts/patch_desktop_scroll.py`、`runtime/hermes-desktop/resources/app.asar`

### 2026-05-20 — EXE 不提示升级 / README 版本过旧
- **现象**：`HermesGo.exe` 不弹更新；`README.txt` 仍写 v0.14.4 但 `__init__.py` 已是 0.14.7；GitHub 只有带日期 zip 名
- **错误做法**：只改 runtime `__version__` 不 stamp README；`HERMESGO_AUTO_UPDATE_PROMPT=0` 时静默直接覆盖；Release 只上传 `HermesGo-YYYY...zip` 无 `HermesGo-green-3ui-slim.zip`
- **正确做法**：`build_zip_slim.py` 调 `stamp_readme_release_tag`；交互启动与菜单「更新」均 `ShowPrimaryDeferPrompt`（稍后/取消不升级）；`publish_github_release.py` 同时上传稳定资产名；测试树用 `packaging_sync` → `HermesGo-slim-test-v014`
- **涉及文件**：`packaging_release.py`、`HermesGoBootstrap.cs`、`build_zip_slim.py`、`scripts/publish_github_release.py`

### 2026-05-20 — 多屏打开目录/启动菜单位置与 PrimaryScreen 为空
- **现象**：多显示器下「打开 home/logs」或 `--menu` 启动器跑到主屏、甚至直接崩溃；日志里少见明确栈，多为 `Screen.PrimaryScreen` 空引用
- **错误做法**：`Screen.PrimaryScreen.WorkingArea` 无空判断；`explorer.exe` 裸参数 + `UseShellExecute=false`；把所有 explorer 窗口都 MoveWindow
- **正确做法**：`UiScreenAnchor` 在启动时捕获当前屏，表单用 `Manual` 居中到该屏工作区；`OpenFolder` 用 `UseShellExecute=true` 打开目录并把新开 explorer 挪到目标屏；改 bootstrap 后跑 `HermesGo\scripts\Compile-HermesGoBootstrap.ps1` 并替换包根 `HermesGo.exe`
- **涉及文件**：`create_hermes_go/HermesGoBootstrap.cs`

### 2026-05-20 — 冒烟/打包易踩坑（4 UI）
- **现象**：`smoke_kanban_all_ui` 报 portable root=`E:\AI\hermes\HermesGo`；`build_zip_slim.py` 缺根目录 `.bat`；`patch_desktop_scroll.py` 报 `npx` 找不到；hello 冒烟无 key 却显示 webui OK
- **错误做法**：冒烟不设 `HERMES_PORTABLE_APP_ROOT`；工作区无 `HermesGo/*.bat` 仍打包；子进程裸调 `npx`；把「无 API key」助手提示当成功
- **正确做法**：`run_full_smoke_iter.ps1` 与各 smoke 设置 `HERMES_PORTABLE_APP_ROOT=$appRoot`；打包前 `git checkout -- HermesGo/Hermes*.bat`；patch 用 `shutil.which('npx')` + Windows `shell=True`；hello 需 `app/home/.env` 中 `DEEPSEEK_API_KEY` 或 Dashboard `/env`
- **涉及文件**：`scripts/smoke_*.py`、`scripts/run_full_smoke_iter.ps1`、`build_zip_slim.py`

### 2026-05-19 — Desktop 聊天滚动乱跳（选文字后丢失位置）
- **现象**：选中一段文字阅读，流式更新后视口跳到别处、选区丢失
- **错误做法**：仅调 `kNt`；忽略 `getSelection()`；WebUI `_shouldFollowMessagesOnDomReplace` 用 `nearBottom(1200)`
- **正确做法**：Desktop `MNt`：有选区/指针按下时 `f.current` 锁读、禁止 `g()`/`ResizeObserver` 改 scrollTop；`groupCount` 变化时非 pinned 恢复 `scrollTop`；WebUI：`_hasMessagePaneTextSelection` + `#messages` pointerdown 锁读；`npx @electron/asar pack` 后重启 Desktop
- **涉及文件**：`hermes-webui/static/ui.js`、`HermesGo/runtime/hermes-desktop/resources/app.asar`

### 2026-05-19 — 绿色版把主机 Cursor/开发仓库内容带进 Desktop
- **现象**：U 盘绿色版聊天里出现 `E:\AI\.cursorrules` 或本机 Hermes 开发仓库上下文；数据似乎不在 U 盘
- **错误做法**：gateway/dashboard `WorkingDirectory=runtime/hermes-agent`；工具扫 `E:\AI\hermes` 时 `subdirectory_hints` 仍加载主机 `.cursorrules`；`terminal.cwd: .` 相对开发机目录
- **正确做法**：`Ensure-PortableLayout` 创建 `workspace/`、`webui-data/`、`home/desktop-shell/`，把 `terminal.cwd` 写成绝对 `app/workspace`；导出 `HERMES_PORTABLE_*`、`TERMINAL_CWD`、`HERMES_DESKTOP_USER_DATA_DIR`；`subdirectory_hints._within_portable_root` + `prompt_builder._portable_context_cwd` 限制在 `HERMES_PORTABLE_APP_ROOT` 内
- **涉及文件**：`Start-HermesGo.ps1`、`agent/subdirectory_hints.py`、`agent/prompt_builder.py`、`run_agent.py`

### 2026-05-20 — Windows 强制删除命令路径转义风险
- **现象**：本意只删除占用日志目录，结果工作区出现过度删除（根目录几乎清空）
- **错误做法**：❌ 在 PowerShell 中拼接 `cmd /c rmdir /s /q "\\?\$path"`，并在未再次校验绝对路径时执行强制删除
- **正确做法**：✅ 仅使用 `Remove-Item -LiteralPath` + 白名单校验 + `-WhatIf` 预演；占用文件先按路径精准 `Stop-Process`，禁止使用字符串拼接的 `cmd rmdir` 兜底
- **涉及文件**：`scripts/Invoke-HermesSafeCleanup.ps1`

### 2026-05-22 — 绿色版全入口禁用 WSL（含 cmd hermes）
- **现象**：用户要求 gateway、Dashboard、Desktop、外挂 Chat/cmd、`hermes.cmd` 均不得走 WSL 分支
- **错误做法**：❌ 仅在 bat 上设变量、子进程未继承；❌ 只靠 `is_wsl()` 读 `/proc/version` 而无绿色包兜底
- **正确做法**：✅ `Start-HermesGo.ps1` 统一 `HERMES_DISABLE_WSL=1` + `Add-HermesGreenRuntimeEnv`；`hermes.cmd`/`portable-shell-init.cmd`/`Start-ChatWindow`；`ensure_hermes_green_windows_env()` 于 `main.py`、`run.py`、`tui_gateway/server.py`；`hermes_green_windows_only()` 使 `is_wsl()` 恒 false
- **涉及文件**：`hermes_constants.py`、`Start-HermesGo.ps1`、`Ensure-HermesGoGlobalPath.ps1`、`Hermes*.bat`、`Verify/Switch*.bat/ps1`

### 2026-05-22 — Desktop Shell 里 API Key「丢失」
- **现象**：Dashboard/WebUI 已配好 DeepSeek/Codex；Desktop 内 Windows Shell 或 `hermes` 仍提示未配置 / 无 API key
- **错误做法**：❌ 假定 Electron 子进程继承启动时环境即可；❌ 只靠 `config.yaml` 有 provider 不写 `.env`；❌ Shell 用 `%LOCALAPPDATA%\hermes` 当 `HERMES_HOME`
- **正确做法**：✅ `patch_desktop_portable_shell.py` 让 `terminalShellEnv()` 每次开 Shell 从 `HERMES_HOME/.env` 合并密钥；✅ `cmd /K app/home/portable-shell-init.cmd`；✅ `hermes_cli/main.py` 在 `cmd_chat` 前 `load_hermes_dotenv`；✅ Shell 用 `app/runtime/bin/hermes.cmd`
- **涉及文件**：`scripts/patch_desktop_portable_shell.py`、`Start-HermesGo.ps1`、`hermes_cli/main.py`、`build_zip_slim.py`

### 2026-05-22 — 绿色版须严格即插即用（不写主机；Hermes 不依赖 WSL）
- **现象**：启动 exe 后 Windows Shell 里 `hermes` 提示未配置；或配置/API Key 写到 `%USERPROFILE%\.hermes`、用户 PATH
- **错误做法**：❌ 默认 `Ensure-HermesGoGlobalPath` 写 User PATH/HERMES_HOME；❌ `Ensure-PortableAuthFromProfile` 从 `~/.hermes` 复制；❌ 让绿色版 Hermes 走 WSL/bash（目标用户机通常未装 WSL）；❌ 把「禁用 WSL」理解成限制 Cursor
- **正确做法**：✅ 默认 `HERMESGO_STRICT_PORTABLE=1`（bat/exe/hermes.cmd）；数据仅在 `app/home`；CLI 用 `app/runtime/bin/hermes.cmd`；**全部子进程 + 外挂 cmd** 设 `HERMES_DISABLE_WSL=1`；Python 侧 `ensure_hermes_green_windows_env()` + `is_wsl()` 恒 false；**不要求、不安装 WSL**；需写主机时显式 `HERMESGO_ALLOW_HOST=1`
- **涉及文件**：`Start-HermesGo.ps1`、`Ensure-HermesGoGlobalPath.ps1`、`hermes_constants.py`、`HermesGo*.bat`、`HermesGoBootstrap.cs`、`packaging/config.yaml.slim-default`

### 2026-05-22 — 双击 exe 须一次配齐（勿再跑 bat/手动 setup）
- **现象**：用户希望只执行 `HermesGo.exe` 即完成 home/config/.env、hermes.cmd、shell init、gateway、三件套 UI，仍去跑 `HermesGo.bat` 或 CLI `setup`
- **错误做法**：❌ exe 只起菜单或只开 Desktop 不做 `Initialize-*`；❌ 初始化逻辑散落在 try 块多段、与 bat 不一致；❌ Bootstrap 子进程未带 `HERMESGO_CONFIGURE_ALL`
- **正确做法**：✅ 无 `--menu` 时 `ShouldUseDirectLaunch` → `LaunchPackage` → `Start-HermesGo.ps1`；✅ `Initialize-HermesGoPortablePackage` 统一调用 layout/config/dotenv/wrapper/shell-init + `ensure_portable_exe_ready()`；✅ exe 设 `HERMESGO_LAUNCHED_BY_EXE=1`、`HERMESGO_CONFIGURE_ALL=1`；✅ 写 `app/.hermesgo-ready` 标记；CLI 仍用 `app\runtime\bin\hermes.cmd`（严格模式不写主机 PATH）
- **涉及文件**：`HermesGoBootstrap.cs`、`Start-HermesGo.ps1`、`hermes_cli/portable_bootstrap.py`、`README.txt`

### 2026-05-22 — 启动后命令行找不到 hermes
- **现象**：双击 `HermesGo.exe` / `.bat` 能起三件套，但新开 CMD/PowerShell 输入 `hermes` 报不是内部或外部命令
- **错误做法**：❌ 只改当前进程的 `$env:PATH`；假设 `runtime/bin` 里已有 wrapper 且已写入系统环境变量
- **正确做法**：✅ `Start-HermesGo.ps1` 调用 `scripts/Ensure-HermesGoGlobalPath.ps1`：生成 `app/runtime/bin/hermes.cmd`，把该目录写入 **User** `Path` 并设 `HERMES_HOME=app/home`；全机用 `HERMESGO_PATH_SCOPE=Machine` 且管理员运行；禁用 `HERMESGO_SKIP_GLOBAL_PATH=1`；**新开终端**后执行 `hermes --version`
- **涉及文件**：`HermesGo/scripts/Ensure-HermesGoGlobalPath.ps1`、`Start-HermesGo.ps1`、`build_zip_slim.py`（打包进 `app/scripts/`）

### 2026-05-23 — Dashboard Codex OAuth 一点就报错
- **现象**：`agent.log` 中 `AttributeError: module 'hermes_cli.auth' has no attribute '_generate_codex_pkce'`；Dashboard 点 Codex 登录立即 500
- **错误做法**：`web_server._start_codex_browser_flow` 调用未在 `auth.py` 实现的 `_generate_codex_pkce` / `_start_codex_browser_login_server`
- **正确做法**：在 `auth.py` 实现 `CODEX_BROWSER_REDIRECT_URI`、`_generate_codex_pkce`、`_CodexBrowserLoginServer`、`_start_codex_browser_login_server`、`_build_codex_browser_auth_url`（1455 失败回退 ephemeral 端口）
- **涉及文件**：`runtime/hermes-agent/hermes_cli/auth.py`、`web_server.py`

### 2026-05-23 — app/home/.env 带 UTF-8 BOM 导致 DeepSeek 密钥无效
- **现象**：`.env` 有 `EF BB BF` 前缀，`DEEPSEEK_API_KEY` 读成 `\ufeffDEEPSEEK_API_KEY`；Dashboard/Agent 报无 API Key，但文件里明明有 sk-
- **错误做法**：`Set-Content -Encoding utf8`（PS 5.1 写 BOM）；`Read-DotEnvFile` / `load_dotenv(encoding=utf-8)` 不剥离 BOM
- **正确做法**：`.env` 用 `Write-Utf8NoBomFile`；`Read-DotEnvFile` 对 key `TrimStart(BOM)`；Python `env_loader` 用 `utf-8-sig`
- **涉及文件**：`Start-HermesGo.ps1`、`hermes_cli/env_loader.py`

### 2026-05-23 — Write-PortableShellEnvBootstrap 赋值 `$home` 启动失败
- **现象**：`HermesGo startup failed: 无法覆盖变量 HOME，因为该变量为只读变量或常量`；停在 layout 之后
- **错误做法**：`Write-PortableShellEnvBootstrap` 内 ` $home = Join-Path $AppRoot "home"`（与 PowerShell 自动变量 `$HOME` 冲突）
- **正确做法**：局部变量改用 `$appHome`；`.env` 导入跳过 `HOME` 等保留键
- **涉及文件**：`Start-HermesGo.ps1`

### 2026-05-23 — VS Code ACP 绿色版须便携 hermes.cmd + agent-client-protocol
- **现象**：用户要在 VS Code 里编程；`hermes acp` 报 `ACP dependencies not installed`；扩展用系统 `hermes` 找不到绿色包配置
- **错误做法**：❌ 只文档写 `pip install -e '.[acp]'` 不装进 `runtime/python311`；❌ VS Code 指向 `~/.hermes` 或主机 PATH
- **正确做法**：✅ `ensure_portable_acp_deps()` + `HermesVSCode.bat` 启动 **仅 Microsoft VS Code**（`Find-VSCodeCli`，不找 Cursor）；✅ `app/editor/vscode/hermes-vscode.settings.json`；✅ 打包前 `install_slim_runtime_extras.py`；✅ 扩展 `formulahendry.acp-client`
- **涉及文件**：`Install-HermesVSCode.ps1`、`HermesVSCode.bat`、`packaging/docs/VSCODE-GREEN.md`

### 2026-05-23 — VS Code 绿色版入口命名（非 ACP、非 Cursor）
- **现象**：`HermesVSCodeACP.bat` 不启动 VS Code；用户要「VS Code 绿色版」且名字不带 ACP
- **错误做法**：❌ 只生成 JSON 不 `Start-Process` VS Code；❌ `Find-EditorCli` 包含 Cursor
- **正确做法**：✅ 根目录 `HermesVSCode.bat` + `Install-HermesVSCode.ps1`；✅ 仅 `code` / `Code.exe` 路径；✅ 默认 `-LaunchEditor` 打开 `app\workspace`；文档 `VSCODE-GREEN.md`
- **涉及文件**：`HermesVSCode.bat`、`scripts/Install-HermesVSCode.ps1`

### 2026-05-23 — 打包后自动 sync 到 HermesGo-slim-test-v014 并配 DeepSeek Key
- **现象**：用户期望每次 `build_zip_slim.py` 完成后测试树已是解压好的最新包且 API Key 已写好
- **错误做法**：❌ 设 `HERMESGO_SKIP_TEST_SYNC=1` 后不同步；❌ 只打 zip 不跑 `packaging_sync.py`；❌ Key 只写在对话里
- **正确做法**：✅ `build_zip_slim.py` 结束自动 `sync_test_package_from_zip` → `HermesGo-slim-test-v014\HermesGo`；✅ `packaging/configure_test_package.py` + `packaging/test-deepseek.key`（gitignore）或 `HERMESGO_DEEPSEEK_API_KEY` 写 `app/home/.env` + deepseek `config.yaml`
- **涉及文件**：`packaging_sync.py`、`build_zip_slim.py`、`packaging/configure_test_package.py`

### 2026-05-23 — 完整绿色包禁止首启 pip 下载
- **现象**：用户要求「完整包、开机不下载」；首启仍 `pip install ddgs` / `agent-client-protocol`
- **错误做法**：❌ `build_zip_slim` 对 extras 仅 WARN；❌ 运行时 `ensure_portable_*_deps` 无网仍调 pip
- **正确做法**：✅ 构建前 `install_slim_runtime_extras.py` 失败则中止打包；✅ zip 内须含 `site-packages/acp/` 与 `ddgs/`；✅ 标记 `no_pip_download=1` 且 `HERMESGO_NO_PIP_DOWNLOAD=1`
- **涉及文件**：`packaging/install_slim_runtime_extras.py`、`packaging/verify_zip_offline_extras.py`、`Start-HermesGo.ps1`

### 2026-05-23 — Desktop 仍用 curl 而非联网检索
- **现象**：绿色版已装 optional-skills/ddgs，Desktop 对话里仍大量 `curl` 抓网页
- **错误做法**：❌ 以为「浏览器插件」= Chrome 扩展；❌ 只靠 duckduckgo-search **技能**让模型走 terminal+`ddgs`/`curl`；❌ 不重启 gateway（旧 Python 进程缓存旧 `web_tools`）；❌ 未在系统提示里禁止 curl 搜网页
- **正确做法**：✅ `web_tools.py` 无付费 Key 时 `web_search` 走 `ddgs`；✅ `web.backend: duckduckgo`；✅ `prompt_builder._hermesgo_green_web_search_hint()` 强制优先 `web_search`；✅ 改 duckduckgo skill 优先 `web_search` 而非 curl；✅ 改代码后跑 `app/scripts/Restart-HermesGoServices.ps1` 再开 Desktop；❌ **browser_*** 仍需 Chrome 调试端口
- **涉及文件**：`tools/web_tools.py`、`agent/prompt_builder.py`、`optional-skills/.../duckduckgo-search/SKILL.md`、`scripts/Restart-HermesGoServices.ps1`

### 2026-05-23 — slim 无 web_search 时 Agent 用 curl 极慢
- **现象**：Desktop 问联网问题时出现大量 `curl api.github.com`、超时/审批拒绝；比 Cursor/Trae 慢很多轮
- **错误做法**：❌ 以为未装「插件」；❌ 只靠 terminal+curl；❌ slim 不打包 `optional-skills`、不装 `ddgs`
- **正确做法**：✅ 联网检索优先 `web_search`（配置 `TAVILY_API_KEY` 或 `FIRECRAWL_API_KEY`）；✅ slim 默认打包 `optional-skills` 中 `duckduckgo-search` 等并写 `skills.external_dirs`；✅ 启动时 `ensure_portable_web_search_deps()` 安装 `ddgs`；✅ `HERMES_OPTIONAL_SKILLS=app/optional-skills`
- **涉及文件**：`hermes_cli/portable_bundled_skills.py`、`packaging/config.yaml.slim-default`、`build_zip_slim.py`、`Start-HermesGo.ps1`、`hermes_cli/portable_bootstrap.py`

### 2026-05-23 — CLI 变黑白（NO_COLOR / TERM=dumb）
- **现象**：HermesGo Chat/CLI 从金色 kawaii 变成黑白，config 仍是 `display.skin: default`
- **错误做法**：❌ 只改 `skin: default` 不处理环境变量；❌ 以为 Rich 坏了
- **正确做法**：✅ 便携启动清除 `NO_COLOR`、设 `HERMES_FORCE_COLOR=1`、`FORCE_COLOR=1`、`TERM=xterm-256color`；✅ `hermes_cli/colors.py` 在 `HERMES_PORTABLE_APP_ROOT` 下忽略 NO_COLOR；✅ `cli.py` 启动时 pop NO_COLOR；✅ Chat 窗口 cmd 链与 `portable-shell-init.cmd` 同步
- **涉及文件**：`Start-HermesGo.ps1`、`runtime/bin/hermes.cmd`、`hermes_cli/colors.py`、`cli.py`

### 2026-05-23 — build_zip_slim 无 npx 时桌面 patch 失败
- **现象**：`build_zip_slim.py` 报 `FileNotFoundError: npx not found`；机器未装 Node.js
- **错误做法**：❌ 强行要求每次 npx patch；❌ 无 marker 检测仍中止打包
- **正确做法**：✅ `app.asar` 已含 `scrollToIndex`/`mergePortableCredentialsIntoShellEnv` marker 时跳过 `patch_desktop_*.py`；✅ 新环境需 Node/npx 或预 patch 后再打包
- **涉及文件**：`build_zip_slim.py`、`scripts/patch_desktop_scroll.py`

### 2026-05-23 — Desktop 改 web_search 后仍 curl：Dashboard(9119) 未重启
- **现象**：`web_search`/`ddgs` 已装进包且 CLI 冒烟 OK，但 Desktop/`tools.show` 只有 18 个工具、无 `web_search`；会话报 `Tool 'web_search' does not exist` 后走 `python3 -c`+urllib
- **错误做法**：❌ 只重启 `gateway run` 或只杀 `Hermes.exe`；❌ `Restart-HermesGoServices.ps1` 仅匹配 `hermes_cli*main.py`（漏掉 `-m hermes_cli.main dashboard`）
- **正确做法**：✅ Desktop WebSocket 走 **Dashboard 进程**（`hermes_cli.main dashboard --port 9119`），必须与 gateway 一并重启；✅ 重启脚本匹配 `hermes_cli.main`/`hermes_cli\\main.py` 并释放 `:9119/:8787`；✅ 用 `scripts/smoke_web_search_desktop.py` 或 `probe_gateway_tools.py` 验证 `tools.show` 含 `web_search`
- **涉及文件**：`scripts/Restart-HermesGoServices.ps1`、`scripts/smoke_web_search_desktop.py`、`tui_gateway/server.py`

### 2026-05-23 — DeepSeek Desktop 仍用 python3 urllib 而非 web_search
- **现象**：Desktop 问联网事实时出现 `python3 -c "import urllib..."`；`web_search` 已装且冒烟通过
- **错误做法**：❌ 只装 ddgs 不加 `deepseek` 到 `TOOL_USE_ENFORCEMENT_MODELS`；❌ 绿色包仍 `tool_use_enforcement: auto`
- **正确做法**：✅ `TOOL_USE_ENFORCEMENT_MODELS` 含 `deepseek`；✅ slim `agent.tool_use_enforcement: true`；✅ `_hermesgo_green_web_search_hint` 禁止 `python3 -c`/urllib 模拟搜索；✅ 改 prompt 后 `Restart-HermesGoServices.ps1` + **新开会话**
- **涉及文件**：`agent/prompt_builder.py`、`packaging/config.yaml.slim-default`

### 2026-05-23 — web_search 须打进 zip 且构建时冒烟
- **现象**：用户要求「把 web_search 安装进去」；Desktop 仍 curl；测试树与 zip 不一致
- **错误做法**：❌ 只改 `web_tools.py` 不重打 zip；❌ `verify_zip` 仅查 `ddgs/` 不查 `web_tools` 与 `web_search_tool` 可调用；❌ sync 时 `kanban.db` 被占用仍强删整棵 `app/`
- **正确做法**：✅ `packaging/verify_zip_web_search.py` 在 `build_zip_slim` 后校验 zip 含 `_ddgs_search` 且 `web_search_tool` 冒烟通过；✅ `.hermesgo-green` 标记 `bundled_extras=...,web_search`；✅ `ensure_portable_web_search_installed()` 首启合并 ddgs+config+`check_web_api_key`；✅ sync 前先 `Restart-HermesGoServices.ps1`，锁库失败则只覆盖 `runtime/hermes-agent` 等关键文件
- **涉及文件**：`packaging/verify_zip_web_search.py`、`portable_bootstrap.py`、`build_zip_slim.py`

### 2026-05-20 — Kanban Dashboard 插件静态资源缺失导致 4UI 冒烟失败
- **现象**：`smoke_kanban_all_ui.py` 中 Desktop 远端检查 `/dashboard-plugins/kanban/dist/index.js` 与 `style.css` 返回 404
- **错误做法**：❌ 仅恢复 `plugins/kanban/dashboard/manifest.json` 与 `plugin_api.py`，遗漏 `dashboard/dist/*`
- **正确做法**：✅ 确保 `runtime/hermes-agent/plugins/kanban/dashboard/dist/index.js` 与 `dist/style.css` 在打包前存在；若工作区恢复后缺失，先从可用 release 包回填再执行 `build_zip_slim.py`
- **涉及文件**：`runtime/hermes-agent/plugins/kanban/dashboard/dist/*`、`build_zip_slim.py`、`scripts/smoke_kanban_all_ui.py`
