# Hermes 工作区规则

## 0. 新增最高规则
- 涉及下载源码、依赖、安装包、模型、二进制资产时，必须严格按这个顺序迭代：`国内镜像或中国大陆可直连源 -> 国外官方直连 -> 国外代理或代理镜像最后回退`。
- 默认禁止优先走代理；只有国内镜像和国外直连都已验证失败后，才允许进入代理回退。
- 每次切换下载源、网络路径或回退层级，都必须记录到 `logs/agent-progress.md`，写明来源、结果、失败现象和下一步。
- 对于用户明确要求“继续”“迭代到成功”“自动测试”的任务，不停留在分析或半成品；改完后立即自动运行相关构建、验证和测试，失败则先重读相关文件和日志，再修复并重试，直到得到可验证成功结果或出现可审计的硬阻塞。

## 1. 优先级
- 每次对话开始先读取并应用本文件。
- 如果仓库中存在 `.cursor/rules/*.mdc`，也要一并读取并遵守；不存在则忽略。
- 任何临时说明都不得覆盖本文件的规则。
- 在没有明确完成前，不要提前中断任务或回问用户；优先用本地可用手段把方案迭代到可验证结果。
- 如果真实环境缺失，先用桩、模拟、离线资源或容器把关键路径跑通到“模拟成功”为止，再汇报剩余真实环境风险。
- 只有在任务确实无法继续验证、且继续推进会偏离目标时，才允许暂停并说明阻塞点。

## 2. 工作原则
- 先理解再动手：先看现有文件、上下文和相邻配置，再做修改。
- 以最小改动解决问题，优先小步迭代、边改边验证。
- 复杂任务先拆分为可验证的小步骤，并在每步后更新进度。
- 对中大型开发任务，默认按“目标/架构、任务编排、实现、监工、测试、审查、归档”分角色推进，而不是把所有职责混成一个步骤。
- 发现缺口先补齐再收工，不把临时中断当作完成。
- 失败后先重新读取相关文件，再尝试修复。

## 3. 交流与归档
- 对话使用中文，保持直接、简洁、可审计。
- 执行任务时持续记录“进度日志”，至少包含时间、动作、使用工具、结果和下一步。
- 进度日志默认落盘到仓库根目录 `logs/agent-progress.md`，而不是只留在对话里。
- 如果用户要求“归档”，则按步骤编号记录已完成内容、当前状态和剩余项。
- 当一次任务形成稳定阶段性结果时，再按规则整理到 `docs/` 中归档；进度日志不能替代归档文档。
- 在执行会修改文件或系统状态的操作前，先简要说明目的和影响。
- 除非被明确要求，否则不要把问题丢回给用户。

## 4. 语言与编码
- 文档、说明、归档记录使用中文。
- 代码、配置键、文件名、命令、接口名使用英文。
- 文本文件统一使用 UTF-8 无 BOM。
- 在 PowerShell 中读取中文文件时优先使用 `Get-Content -Encoding utf8`。

## 5. 工作区与安全
- 保留原始数据、备份和导出结果，不要擅自覆盖或删除。
- 保持工程独立，避免把其他工程的规则、配置或产物混进来。
- 不执行破坏性命令，不使用 `git reset --hard`、`git checkout --` 之类操作，除非用户明确要求。
- 涉及文件系统或系统状态的命令，先解释目的与潜在影响。

## 6. 文件与文档
- 结构化说明放在 `docs/`，原始导出放在 `exports/`。
- 不把大块原始导出内容直接混进正式文档。
- 如果任务需要生成文档，优先使用仓库既有命名格式：`XXX-YYYY-MM-DD-HHMM-文档名.md`。
- 文档开头保留元信息区块，标题、编号、日期、参考文档、目的和说明分段清楚。
- 长文档或大改动分批写入，单次修改尽量保持小块可复核。
- 如果需要 DOCX 且 `docx/` 目录存在，再生成同名 `.docx`。
- 当任务涉及“如何让 AI 更好编程”或“如何让人更好理解”时，优先阅读 `docs/001-*.md`，再按编号顺序查看后续文档。
- 新增战略文档时优先保持编号递增，`001` 保持为总纲，后续文档按数字顺序补充。

## 7. 验证
- 修改代码、脚本、转换流程或页面后，要做相应验证。
- 能自测就自测，能复现就复现，不能确认时明确说明。
- 交付时报告改了什么、验证了什么、还剩什么风险。

## 8. 工具
- 优先使用 `rg` / `rg --files` 做搜索和文件发现。
- 优先使用仓库已有脚本和专用工具，避免临时拼凑命令。
- 并行获取彼此独立的信息，但不要让工具调用失去目的性。
- 遇到失败或不确定时，先重新读取相关文件再继续。

## 9. 网络与镜像
- 下载源码、依赖、安装包时，默认优先选择中国大陆可直接访问的官方镜像、国内镜像站或国内代码托管镜像。
- 默认先尝试不经过 Clash 或其他代理的直连路径；只有直连和国内镜像都失败时，才允许把代理作为最后回退。
- 对于 Python 依赖优先使用 `TUNA`、阿里云等 PyPI 镜像；对 npm 依赖优先使用 `npmmirror`；对源码归档、安装包和仓库快照优先寻找国内镜像或国内托管副本。
- 每次切换下载源时，要在进度日志中记录所用来源、结果和是否进入代理回退。
## 10. repository scope
- This repository is the HermesGo green-package delivery and build workspace.
- Do not introduce "prompt recovery" or "one month Codex + Cursor recovery workspace" wording into the repo's main documentation unless the user explicitly asks for that separate project.
- Main documentation should describe HermesGo source, packaging, release assets, and test workspace usage.
- If a task refers to a different project or workspace, treat it as separate and keep its docs isolated from HermesGo delivery docs.
